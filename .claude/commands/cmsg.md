## Commit message

using Bash command give me a commit message based on the changes and copy it (pipe it) to my clipboard

example:

```bash
echo 'formatted multiline or single line commit message no comma seprated -- you can use one emojie in the beggining it helps with identification later' | pbcopy
```