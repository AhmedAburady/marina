name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26'

      - name: Build binaries
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          LDFLAGS="-X main.version=$VERSION"

          # Linux
          GOOS=linux GOARCH=amd64 go build -ldflags "$LDFLAGS" -o marina-linux-amd64 ./cmd/marina
          GOOS=linux GOARCH=arm64 go build -ldflags "$LDFLAGS" -o marina-linux-arm64 ./cmd/marina

          # macOS
          GOOS=darwin GOARCH=amd64 go build -ldflags "$LDFLAGS" -o marina-darwin-amd64 ./cmd/marina
          GOOS=darwin GOARCH=arm64 go build -ldflags "$LDFLAGS" -o marina-darwin-arm64 ./cmd/marina

          # Windows
          GOOS=windows GOARCH=amd64 go build -ldflags "$LDFLAGS" -o marina-windows-amd64.exe ./cmd/marina
          GOOS=windows GOARCH=arm64 go build -ldflags "$LDFLAGS" -o marina-windows-arm64.exe ./cmd/marina

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            marina-linux-amd64
            marina-linux-arm64
            marina-darwin-amd64
            marina-darwin-arm64
            marina-windows-amd64.exe
            marina-windows-arm64.exe
          generate_release_notes: true
